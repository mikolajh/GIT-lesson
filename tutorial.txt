git init
git config --global user.name "mikolajh"
git config --global user.email "test@example.com"

git status
git add .
git commit -m "name of commit"

git remote add origin https://github.com/mikolajh/GIT-lesson.git
git push -u origin master